<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/material_primary</item>
        <item name="colorAccent">@color/material_accent</item>
    </style>

    <style name="HubButton">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:colorBackground">?attr/colorPrimary</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="backgroundTint">@color/material_primary</item>
        <item name="android:textColor">@android:color/primary_text_dark</item>
    </style>

    <style name="HubTitle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_centerHorizontal">true</item>
    </style>

    <style name="HubButtonAccent" parent="HubButton">
        <item name="backgroundTint">@color/material_accent</item>
    </style>
</resources>